const formatThousands = (n) => {
  if (n <= 99) {
    return n.toString();
  } else if (n > 99 && n < 1e3) {
    return "99+";
  } else {
    return "1K+";
  }
};
const getVariantClassName = (variant) => {
  if (!variant) {
    return "gds-badge--neutral";
  }
  return `gds-badge--${variant}`;
};
const calculateBadgeProps = (props) => {
  const {
    count,
    getAriaLabelText,
    specialCharBefore,
    specialCharAfter,
    style: styleProp,
    variant,
    className,
    ...rest
  } = props;
  const withSpecialChar = specialCharBefore != null || specialCharAfter != null;
  const displayText = withSpecialChar ? `${specialCharBefore != null ? specialCharBefore : ""}${count}${specialCharAfter != null ? specialCharAfter : ""}` : formatThousands(count);
  const screenReaderText = getAriaLabelText == null ? void 0 : getAriaLabelText(
    withSpecialChar ? count : displayText,
    specialCharBefore,
    specialCharAfter
  );
  const variantClassName = getVariantClassName(variant);
  const mergedClassName = className ? `${variantClassName} ${className}` : variantClassName;
  return { displayText, screenReaderText, style: styleProp, className: mergedClassName, ...rest };
};
export {
  calculateBadgeProps,
  formatThousands
};
